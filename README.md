# Movie Finder (OMDb)

Recruitment task implementation for a movie search application based on the OMDb API.

## Tech stack

- Next.js (App Router)
- TypeScript
- Tailwind CSS
- ESLint

## Local development

```bash
npm install
npm run dev
```

App runs at [http://localhost:3000](http://localhost:3000).

## Environment variables

Create `.env.local` file:

```bash
OMDB_API_KEY=your_api_key
```

## Scripts

- `npm run dev` - start development server
- `npm run build` - create production build
- `npm run start` - run production server
- `npm run lint` - run linter

## Current status

Project bootstrap is complete. Core features (search, details, favorites) are implemented in the next steps.
